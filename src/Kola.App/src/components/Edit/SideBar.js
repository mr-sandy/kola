import React from 'react';

const style = {
    position: 'relative',
    height: '100%',
    width: '60px',
    backgroundColor: '#333',
    color: '#eee',
    float: 'left',
    zIndex: '200',
    paddingTop: '4px',
    overflow: 'hidden'};

const SideBar = props => {

    return (<div style={style}>
        
    </div>
    );
}

export default SideBar;