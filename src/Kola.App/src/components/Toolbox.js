import React from 'react';

const style = {
    backgroundColor: 'rgba(0,0,0,0.4)',
    height: '100px',
    width: '100px'
};

const Toolbox = props => {

    return (<div style={style}>
        
    </div>
    );
}

export default Toolbox;