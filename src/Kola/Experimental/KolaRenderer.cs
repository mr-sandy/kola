namespace Kola.Experimental
{
    // Is it possible to use injection to write an integration test that generates the html for a whole page?
    public class KolaRenderer
    {
        public IKolaResponse Render(IKolaPage page)
        {
            return null;
        }
    }
}
