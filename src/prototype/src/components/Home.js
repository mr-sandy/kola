import React, { Component } from 'react';
import { Link } from 'react-router';

const Home = () => {
    return (
        <div>
            <Link to="/edit">Edit</Link>
        </div>
    );
};

export default Home;
